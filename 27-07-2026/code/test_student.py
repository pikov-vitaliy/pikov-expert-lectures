"""Проверка ВАШЕЙ работы — файла step3_student.py.

Это те же самые проверки, что и в test_journal.py. Разница одна: там они
применяются к эталону step3_fixed.py, а здесь — к вашему step3_student.py.
Ни одну проверку переписывать не нужно, они наследуются целиком.

    py -m unittest -v test_student.py

КАК ЭТИМ ПОЛЬЗОВАТЬСЯ

1. Запустите ДО того, как что-то исправите. Проверки станут красными —
   по одной на каждый дефект. Это нормально и это хорошо: значит, проверки
   действительно что-то проверяют. Тест, который никогда не падал,
   ничего не доказывает.

2. Исправляйте дефекты в step3_student.py по одному и после каждого
   запускайте эту команду снова. Красных становится меньше.

3. Когда все проверки позеленели — задание 3 выполнено. Вот теперь можно
   открыть step3_fixed.py и сравнить свой вариант с эталоном.

ЕСЛИ ВИДИТЕ ОШИБКУ ВРОДЕ «AttributeError: module has no attribute ...»
Это тоже красный результат, просто другого вида: проверка не нашла в вашем
файле функцию, которую ожидала.

Чаще всего это НЕ ошибка, а подсказка: пока вы не добавили нужную функцию.
Устраняя Д1 и Д2, вы добавите в файл две новые функции —
`get_admin_token()` (читает токен из окружения) и `mask()` (возвращает
заглушку для журнала). Пока их нет, соответствующие проверки будут
падать именно с таким сообщением.

Если же функция у вас есть, а ошибка осталась — проверьте, не переименовали
ли вы существующую: имена `connect`, `login`, `find_public`, `export_report`
и константа `EXPORT_DIR` должны остаться прежними, меняется только то,
что у них внутри.
"""

from __future__ import annotations

import unittest

import step3_student
import test_journal


class StudentSecurityRegressions(test_journal.TestSecurityRegressions):
    """Те же регрессионные проверки, но применённые к вашему файлу."""

    module = step3_student


# Функциональные тесты журнала (step2_class.py) от вашей правки не зависят,
# поэтому здесь они не дублируются — они уже есть в test_journal.py.

if __name__ == "__main__":
    unittest.main(verbosity=2)

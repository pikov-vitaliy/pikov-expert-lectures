# Инструменты нужны только для автоматических проверок.
# Сам практикум работает на стандартной библиотеке Python 3.11+.
#
# Диапазоны удобны для занятия, но не дают воспроизводимой сборки.
# В рабочем проекте используйте lock-файл с точными версиями и хешами.

ruff>=0.5,<1.0
bandit>=1.7,<2.0
pip-audit>=2.7,<3.0
pytest>=8.0,<10.0

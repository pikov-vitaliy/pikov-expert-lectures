# Материалы занятия — macOS

Курс «Специалист по процессам разработки безопасного программного обеспечения».

Тема дня: **история и классификация языков, shift-left, компоненты программного стека и безопасная работа с SQL на примере реестра компонентов.**

Эта инструкция — версия `ЧИТАТЬ-ПЕРВЫМ.md` для **macOS**. Если вы на Windows, откройте `ЧИТАТЬ-ПЕРВЫМ.md`: команды для Windows другие.

## С чего начать

1. Откройте `index.html` в браузере — это основная лекция дня.
2. Прочитайте `materials/konspekt.md` — текстовый конспект двух лекционных блоков.
3. Работайте по `materials/praktikum.md` — три последовательных практических блока.
4. Перейдите в `code/` и выполняйте команды только в указанном порядке.
5. В самостоятельной части изменяйте только `code/step3_student.py`.

## Что требуется

- macOS с терминалом (Терминал.app, iTerm2 или другой);
- Python 3.11 или новее;
- встроенный модуль Python `sqlite3`;
- SQLite с поддержкой таблиц `STRICT`;
- текстовый редактор или IDE.

Сторонние пакеты и доступ в интернет для выполнения практикума не нужны. Интернет требуется только для перехода к внешним нормативным и методическим источникам.

## Проверьте окружение

Откройте Терминал и выполните:

```bash
python3 --version
python3 -c "import sqlite3; print(sqlite3.sqlite_version)"
```

Первая команда должна показать Python 3.11 или новее. Вторая печатает версию встроенного SQLite, например `3.45.3`; нужна версия 3.37.0 или новее. Сравнивайте числа по частям: у `3.45.3` вторая часть — 45, это больше 37, версия подходит; у `3.35.5` вторая часть — 35, это меньше 37, версия не подходит. На версии ниже 3.37.0 таблицы `STRICT` не создадутся: `step1_schema.py` завершится ошибкой `near "STRICT": syntax error`. Решение — установить Python 3.11 или новее: вместе с ним обновится и встроенный SQLite.

> [!warning] ⚠️ Не используйте встроенный Python macOS
> Команда `/usr/bin/python3` обычно указывает на устаревшую версию. Проверьте, какой именно Python запускается:
>
> ```bash
> which python3
> ```
>
> Если путь `/usr/bin/python3` — установите свежий Python одним из способов ниже.

## Если Python ещё не установлен

### Способ 1: официальный установщик

1. Скачайте установщик с [https://www.python.org/downloads/](https://www.python.org/downloads/) — нужна версия 3.11 или новее.
2. Запустите `.pkg`-файл и следуйте инструкциям установщика.
3. Закройте и снова откройте Терминал, затем проверьте:

```bash
python3 --version
```

### Способ 2: Homebrew

Если у вас установлен [Homebrew](https://brew.sh/):

```bash
brew install python@3.12
```

После установки проверьте:

```bash
python3 --version
```

## Быстрый старт (macOS)

Из корня комплекта откройте Терминал и выполните:

```bash
cd code
python3 ./step1_schema.py
python3 ./demo_unsafe_query.py
python3 ./step2_secure_queries.py
python3 -m unittest -v test_student.py
```

После первого тестового прогона откройте `step3_student.py`, реализуйте требуемые операции и повторяйте:

```bash
python3 -m unittest -v test_student.py
```

Подробный порядок, вопросы и контрольные точки приведены в `materials/praktikum.md`.

## Как увидеть разницу между красным и зелёным прогоном

По умолчанию `test_student.py` проверяет ваш рабочий файл `step3_student.py`.
Публичный архив предназначен и для самостоятельного повторения, поэтому в нём
есть `step3_student_solution.py`. Открывайте готовое решение только **после собственной попытки**: сначала сохраните исходный RED, реализуйте функции и
зафиксируйте собственный результат. Для оцениваемого занятия преподаватель
выдаёт комплект без файла решения до завершения задания.

В комплекте есть два дополнительных файла, которые можно подключить к тем же
тестам, не изменяя `test_student.py`:

| Файл | Назначение |
|---|---|
| `step3_student.py` | Ваш рабочий файл (красный до выполнения, зелёный после) |
| `step3_student_template.py` | Заглушки с закомментированными подсказками |
| `step3_student_solution.py` | Готовое решение (всегда зелёное) |

Тесты умеют переключаться между модулями через переменную окружения
`REGISTRY_MODULE`. Это штатный механизм, встроенный в `test_student.py` —
менять сам файл тестов не нужно.

### macOS (zsh/bash)

```bash
# Прогнать тесты против готового решения (должен быть зелёный):
REGISTRY_MODULE=step3_student_solution python3 -m unittest -v test_student.py

# Вернуться к своему рабочему файлу — переменная действует только
# на один запуск, сбрасывать не нужно.

# Или задать на всю сессию (не забудьте сбросить потом):
export REGISTRY_MODULE=step3_student_solution
python3 -m unittest -v test_student.py
unset REGISTRY_MODULE
```

> [!warning] ⚠️ Не забывайте сбрасывать переменную
> Если вы использовали `export`, переменная останется в сессии.
> Проверьте: `echo $REGISTRY_MODULE` — если вывод не пустой,
> сбросьте: `unset REGISTRY_MODULE`.

## Просмотр файлов в Терминале

На Windows используется PowerShell с командами `Get-Content` и `Get-ChildItem`. На macOS вместо них работают стандартные команды:

| Действие | macOS (zsh/bash) |
|---|---|
| Просмотр файла | `cat ./schema.sql` |
| Список файлов | `ls ./schema.sql ./seed.sql` |
| Список всех `.py`-файлов | `ls ./*.py` |

## Важные предупреждения

> [!warning] ⚠️ `demo_unsafe_query.py` намеренно уязвим
> Файл нужен только для локальной демонстрации CWE-89 на фиксированной строке и базе в памяти. Не копируйте функцию небезопасного поиска в рабочий код и не подключайте демонстрацию к реальным данным.

> [!important] ❗ Не меняйте критерии приёмки
> В самостоятельной части редактируйте только `step3_student.py`. Не изменяйте `test_student.py`, `schema.sql` и `seed.sql`, иначе зелёный результат не будет независимым свидетельством.

> [!note] ✏️ Повторный запуск шага 1
> `step1_schema.py` не перезаписывает существующую базу. Если `components.db` уже создана, укажите новое имя:
>
> ```bash
> python3 ./step1_schema.py components-02.db
> ```

> [!note] ✏️ Учебный реестр не равен промышленной SCA/SBOM-системе
> Практикум показывает ограничения целостности, безопасные запросы, транзакции, аудит и минимальные полномочия. Он не выполняет автоматическое обнаружение зависимостей, проверку происхождения, поиск уязвимостей или лицензионное заключение.

## Тематические прогоны тестов

```bash
python3 -m unittest -v test_student.AddComponentTests
python3 -m unittest -v test_student.SearchTests
python3 -m unittest -v test_student.OrderingTests
python3 -m unittest -v test_student.TransactionTests
python3 -m unittest -v test_student.LeastPrivilegeTests
```

## Если что-то пошло не так (macOS)

### `python3: command not found`

Python 3 не установлен или не добавлен в `PATH`. Установите Python по инструкции выше.

### `sqlite3.OperationalError: near "STRICT": syntax error`

Подключённая библиотека SQLite не знает ключевого слова `STRICT`. Проверьте:

```bash
python3 --version
python3 -c "import sqlite3; print(sqlite3.sqlite_version)"
```

Если вторая команда печатает версию ниже 3.37.0 — установите Python 3.11+.

### `NotImplementedError` при первом прогоне `test_student.py`

Это ожидаемая стартовая точка RED: функции ещё не реализованы. 14 ошибок при первом запуске — норма.

---

## Маршрут дня

| Время   | Блок        | Результат                                                     |
| ------- | ----------- | ------------------------------------------------------------- |
| 09:30–11:00 | Лекция 1    | классификация языков и карта связанных рисков                 |
| 11:10–12:40 | Лекция 2    | модель архитектуры и угроз реестра                            |
| 13:30–15:00 | Практика 1  | созданная база и анализ ограничений схемы                     |
| 15:10–16:40 | Практика 2  | объяснение CWE-89 и безопасных запросов                       |
| 16:50–18:20 | Практика 3  | самостоятельная реализация с зелёными тестами                 |

## Вопрос дня

> Как сделать так, чтобы реестр компонентов не только хранил данные, но и сохранял их целостность, отделял данные от команд и оставлял проверяемые свидетельства безопасной разработки?

Запишите ответ до начала занятия и вернитесь к нему после полного прогона `test_student.py`.

## Основные источники

- [ГОСТ Р 56939-2024 — официальная карточка Росстандарта](https://protect.gost.ru/gost/details/f3818925-a96f-4f55-96e9-46b44720ee64)
- [NIST SP 800-218 — SSDF Version 1.1](https://csrc.nist.gov/pubs/sp/800/218/final)
- [MITRE CWE-89 — SQL Injection](https://cwe.mitre.org/data/definitions/89.html)
- [OWASP SQL Injection Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html)
- [Python documentation — `sqlite3`](https://docs.python.org/3/library/sqlite3.html)
- [SQLite — Foreign Key Support](https://www.sqlite.org/foreignkeys.html)
- [SQLite — URI Filenames and read-only mode](https://www.sqlite.org/uri.html)

Адрес онлайн-версии после публикации: **https://29-07-2026.pikov.expert/**

Преподаватель: Пиков Виталий Александрович.
